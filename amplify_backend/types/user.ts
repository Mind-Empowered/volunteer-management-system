export enum UserRole {
    ADMIN = 'admin',
    VOLUNTEER = 'volunteer',
  }
  
  export enum UserStatus {
    APPROVED = 'approved',
    PENDING = 'pending',
    REJECTED = 'rejected',
  }
  